The TopicRegex class matches topics based upon regular expressions. The topics must be strings.

{{{
Regex reg = new Regex("^a");
TopicRegex match = new TopicRegex(reg);

OnMessage<MyObject> onMsg=delegate { };
IProcessContext context = ... resolve context
context.Subscribe(match, onMsg);
}}}